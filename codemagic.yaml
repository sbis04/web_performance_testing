workflows:
  flutter-web:
    name: Performance testing
    environment:
      flutter: dev
    scripts:
      - name: Get flutter packages
        script: |
          cd . && flutter packages pub get
      - name: Run benchmarks
        script: |
          flutter test --machine > report.json test/run_benchmarks.dart
    publishing:
      email:
        recipients:
          - sbis1999@gmail.com
